# 実装・表示文言・未実装理由

この記録は実装時点の状況を示します。元の洗い出し40項目は生成AIを使って作成されています。本人だけで作成した成果物としては扱えません。「生成AIに洗い出しを行わせない」が適用条件なら、この洗い出しはその条件を満たしません。履歴を削除せず、担当者に確認のうえ本人による作業を行ってください。

## 8観点の配分

| 観点 | 件数 |
|---|---:|
| 在庫・状態 | 6 |
| 人の状態 | 5 |
| 重複・多重 | 4 |
| 入力値 | 8 |
| 順序 | 4 |
| 時間 | 4 |
| 失敗時 | 5 |
| 不正操作 | 4 |

合計40件。入力値は8件（20%）。この配分は既存のAI生成案の整理であり、今回新たに本人の洗い出しを代行した記録ではありません。

## 項目別の対応

表示は現在のアプリの文言。未実装の表示は「案」とし、実装済みと区別します。同一状況で複数エラーがあるときは先に検知したものを表示します。

| No. | 状況／対応 | 検知時点・表示文言／残理由 |
|---|---|---|
| 1 | 存在しない端末・不正ID／実装 | 申請時・確定時。「対象の端末が見つかりません。」範囲外は「IDは1〜9223372036854775807の整数で入力してください。」 |
| 2 | 貸出中／実装 | 申請・確定時。「この端末は現在貸出中です。別の端末を選択してください。」 |
| 3 | 修理・廃棄／実装 | 申請・確定時。「この端末は貸出対象外、修理中または廃棄済みです。」 |
| 4 | 在庫0／実装 | list表示時。「現在、貸出可能なPCはありません。」コンソールにプルダウンはない。 |
| 5 | 対象外種別／実装 | 申請・確定時。「この端末は貸出対象外、修理中または廃棄済みです。」 |
| 6 | 状態不整合／実装 | 申請・確定時。「貸出情報と端末状態に不整合があります。管理者にお問い合わせください。」 |
| 7 | 退職・休職／実装 | 新規貸出の申請・確定時。「現在の社員状態では新規貸出できません。」返却は受け付ける。 |
| 8 | 社員なし／実装 | 社員選択・操作時。「社員情報を確認できません。」 |
| 9 | 上限到達／実装 | 申請・確定時。「貸出は1人1台までです。現在のPCを返却してください。」 |
| 10 | 他人の返却／部分実装 | 申請時「本人の返却対象が見つかりません。」トークン照合あり。実認証は整合性処理を優先したため未実装。 |
| 11 | 確認後の資格変更／実装 | 確定時に最新情報で再判定。「現在の社員状態では新規貸出できません。」 |
| 12 | 確定連打／実装 | 同じ操作IDの確定時、二重登録せず保存済みの貸出完了文言を表示。コンソールなのでボタン無効化は非該当。 |
| 13 | 同じ端末の競合／実装 | 確定を直列化。後続は「この端末は現在貸出中です。別の端末を選択してください。」 |
| 14 | 同じ社員の並行申請／実装 | 確定を直列化。後続は「貸出は1人1台までです。現在のPCを返却してください。」 |
| 15 | 二重返却／実装 | 同一操作IDは保存済み結果を表示。別IDで返却済みなら「この貸出は返却済みです。」 |
| 16 | 端末未入力／実装 | コンソールで整数変換時。「IDは整数で入力してください。」 |
| 17 | 日付未入力／実装 | 申請時。「有効な日付（YYYY-MM-DD）を入力してください。」未入力専用文言は優先度を下げ、形式エラーと共用。 |
| 18 | 目的未入力・空白／実装 | 申請・確定時。「利用目的は1〜100文字で入力してください。」 |
| 19 | 目的101文字以上／実装 | 申請・確定時。「利用目的は1〜100文字で入力してください。」 |
| 20 | 不正日付／実装 | 申請・確定時。「有効な日付（YYYY-MM-DD）を入力してください。」 |
| 21 | 日付範囲外／実装 | 申請・確定時。「返却予定日は明日から90日以内で入力してください。」 |
| 22 | 特殊文字／実装 | 絵文字・HTMLは平文として保存・表示。制御文字は「利用目的に制御文字は使用できません。」Web用エスケープはコンソールのため非該当。 |
| 23 | 候補外ID／実装 | 範囲外はNo.1、存在なし・対象外はNo.1,5の文言。SQLはパラメーター化。 |
| 24 | 借りていない貸出の返却／実装 | 返却申請時。「本人の返却対象が見つかりません。」 |
| 25 | 古い返却を再送／実装 | 返却済みの貸出IDでは現在の端末を変更しない。「この貸出は返却済みです。」 |
| 26 | 貸出中の修理・廃棄変更／未実装 | 管理者の権限・操作画面が未定義であり貸出／返却を優先。将来の変更確定時の表示案：「貸出中のため変更できません。先に返却処理を行ってください。」 |
| 27 | 返却時の状態不整合／実装 | 返却確定時。「貸出情報と端末状態に不整合があります。管理者にお問い合わせください。」 |
| 28 | 延滞／部分実装 | 判定・返却受付は実装。専用警告はデータ更新処理を優先し未実装。list表示案：「返却期限を過ぎています。速やかに返却してください。」 |
| 29 | 延滞中の貸出／実装 | 申請・確定時。「返却期限を過ぎています。先に返却してください。」対象端末の状態チェックが先にエラーになる場合あり。 |
| 30 | 日またぎ／実装 | 確定時に日本時間で再検証。「返却予定日は明日から90日以内で入力してください。」専用文言は範囲エラーと共用。 |
| 31 | 休業日／未実装 | 会社カレンダー未提供。将来の申請・確定時の表示案：「指定日は休業日です。受付可能な日を選択してください。」 |
| 32 | 貸出更新途中失敗／部分実装 | 全更新のロールバックを確認。SQLiteエラー時はNo.34の共通案内。ロールバック確定後の専用文言は共通処理を優先し未実装。案：「貸出処理に失敗しました。貸出は登録されていません。」 |
| 33 | 返却更新途中失敗／部分実装 | 全更新のロールバックを確認。SQLiteエラー時はNo.34の共通案内。専用文言は未実装（No.32と同理由）。案：「返却処理に失敗しました。返却は完了していません。」 |
| 34 | DB失敗・タイムアウト／部分実装 | コマンド処理中のDBエラー：「DB処理を完了できませんでした。再申請せず、同じ操作IDで結果を照会してください。」ロックタイムアウト試験済み。起動時DB失敗の案内・全障害試験は未対応。まず更新中の整合性を優先。 |
| 35 | 完了結果を受け取れない／部分実装 | 同じ操作IDで照会・再確定すると保存済み完了文言。ネットワークを使わない構成のため実通信断は未試験。 |
| 36 | セッション切れ／未実装 | 実認証を後回しにしたためセッションなし。将来の表示案：「セッションの有効期限が切れました。再ログインして処理結果をご確認ください。」 |
| 37 | 完了済み確認の再送／実装 | confirmコマンドの再実行は保存済み結果。ブラウザバック自体は非該当。 |
| 38 | 確認スキップ／実装 | トークンなし確定は「本人の確認情報が無効です。申請から操作してください。」URL自体は非該当。 |
| 39 | 他人ID／部分実装 | 操作主体とトークンの所有者不一致は拒否。「本人の確認情報が無効です。申請から操作してください。」社員選択は認証ではない。実認証未実装の理由はNo.10と同じ。 |
| 40 | 内容すり替え／実装 | 確定APIは入力内容を受け取らずDBに保存した内容で処理。変更するには新しい確認が必要。通常利用で内容すり替えは成立しない。直接DB改ざんの防御は対象外。 |

## 検証・修正

- ID桁あふれは範囲チェックを追加し、異常終了からエラー表示に修正。
- 並行テストはBarrierで開始を合わせ、2人の同時貸出・同一社員の並行申請・二重送信を検証。
- 23テストが合格。入力のテストだけではなく、トランザクション、再送、状態不整合、権限照合、日またぎも検証。
- これは検証した条件での結果。認証・管理機能・全障害シナリオまで完成したとは主張しない。
- 本記録の表示文言は現行コンソールの実装を示す。既存PDFは設計案のままで、専用文言との差分は上表を参照。
